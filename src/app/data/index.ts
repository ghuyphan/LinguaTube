// Grammar pattern data exports
// Data is lazy-loaded by GrammarService to keep initial bundle size small

// Note: These are NOT re-exported statically to avoid bundling all data upfront.
// Use GrammarService.loadPatterns() or GrammarService.preloadPatterns() instead.

// Individual files:
// - grammar-ja.ts: Japanese JLPT N5-N1 patterns (828 patterns)
// - grammar-ko.ts: Korean 1-6 patterns (744 patterns)  
// - grammar-zh.ts: Chinese grammar patterns (691 patterns)
